class Solution {
    public int myAtoi(String s) {
        String
        for(int i = 0; i < s.length(); i++){
            if(s.charAt(i) >='0' && s.charAt(i) <= '9'){

            }
        }
    }
}