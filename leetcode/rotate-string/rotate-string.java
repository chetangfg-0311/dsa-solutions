class Solution {
    public boolean rotateString(String s, String goal) {

        if(s.length() != go) 
        s = s+s;

        if(s.contains(goal)){
            return true;
        }

        return false;
    }
}